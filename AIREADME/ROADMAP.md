# ROADMAP：ThermalPulse

## Now（当前焦点）

- 产品范围暂定为 macOS 26 以上 Apple Silicon MacBook Pro，并已收敛为菜单栏单面板。状态栏以固定 23 pt 高、10 pt 字形的模板图片显示 P/E 核心热点；右列按动态风扇数量布局，单风扇垂直居中，双风扇上下显示，更多风扇只在展开面板完整列出。点击后一次展开无滚动的完整面板，展示 P 核、E 核、电池温度、一张固定 5 分钟温度曲线、系统 thermal state、逐风扇 RPM 和 Turbo 倒计时、中途停止。
- Mac16,7、Apple M4 Pro 已实测 2 台风扇、102 个有效 `Tp` 候选、10 个有效 `Te` 候选和两枚电池温度；Mac17,2、Apple M5 已实测 1 台风扇、14 个有效 `Tp` 候选、4 个有效 `Te` 候选和两枚电池温度。两台机器都使用族级动态识别，不照搬公开 key 表。不再扩展内存、SSD、高级传感器、时间范围、导出或额外窗口。
- 已注册 v6 helper 使用 `Ftst` 100 毫秒稳定读回、恢复时重申清除、300 毫秒 active 协调、实际 RPM 105% 可信上限、阶段诊断和 ServiceManagement 稳定注册门禁。用户短测中 `Ftst` 稳定接管约 0.98 秒，实际 RPM 上升约 8.78 秒，主动停止恢复成功。当前源码已升级到协议 v7，增加恢复失败立即重试、未知 `Ftst` 禁止写入、XPC 并发请求隔离和固定前两台风扇摘要；系统 helper 仍为 v6，所以运行 v7 App 时 Turbo 会按协议不兼容禁用。
- 用户已显式注册协议 v4 helper，并完成一次短时 active 与主动停止。独立探针读回两台风扇 mode 1、目标精确等于动态最大值和接近最大值的实际 RPM；停止后 mode 3、`Ftst=0`、lease 删除与 helper 无错误恢复日志均通过。实测暴露 1.35% 实际 RPM 超调和 active 风扇文案错误，工作树已修复并升级为协议 v5。用户随后完成 v5 helper 升级并确认验收通过；系统读回新的 root helper、ServiceManagement 管理、活跃 Mach endpoint 和空租约目录。
- P 核曲线曾因稳定代表 `Tp` key 的 0 °C 哨兵值周期性跌到零。工作树现已在展示边界丢弃 P 核与 E 核 10 至 120 °C 之外的点，并保留时间缺口；用户已确认该修复验收通过。本机与 M5 的完整普通测试均为 80 项中 76 项通过、4 项真实硬件测试按设计跳过、0 项失败；M5 单风扇探针与 12 秒温度族刻画分别通过。

## Next

- 先由用户确认 M5 菜单栏右侧单风扇转速的垂直居中外观。M5 探测时存在的外部风扇控制 App 已由用户退出；如后续要验收 Turbo，仍须先用只读门禁重新确认 Apple 自动模式，再单独授权 helper 注册和测试。Mac16,7 若继续 Turbo 验收，仍需先单独升级 v7 helper，再分别验证 600 秒到期、App 崩溃、XPC 断开、helper 重启和休眠唤醒恢复。

## Later

- 经用户验证确有价值后，增加会话导出、曲线对比和可选本地历史。
- 建立多机型只读传感器目录和逐机型 Turbo 支持矩阵。
- 评估面向高负载任务的标记、注释与前后对比报告。
- 形成签名、notarization、升级与卸载的可重复发布流程。

## 已搁置（+原因）

- 长期自定义风扇曲线：会取代苹果的多传感器自动策略，风险和维护成本高于产品收益。
- 任意 RPM 或永久手动模式：扩大 root 写入面，也破坏固定期限的安全模型。
- 自动触发 Turbo：用户可能在不知情时承受噪声、耗电和机械使用，且监控异常不应触发写操作。
- 云端遥测与账户：当前用户问题完全可以本地解决，引入网络会增加隐私和运维负担。
- 把 CPU 频率作为唯一成功指标：频率还受功耗、活跃核心数和调度影响，应结合真实任务吞吐量判断。

## 已完成基线

- 2026-08-28：建立原生 SwiftUI App、测试目标、隔离的只读 `SMCReadAdapter`、typed sensor model 和一次性扫描 UI。
- 2026-08-28：在 Mac16,7 上以普通权限枚举 2 个风扇，并读回两个风扇的 `flt ` 实际与最大 RPM；不包含任何 SMC 写入。
- 2026-08-28：完成一次枚举后的 1 Hz 动态白名单采样、每序列 3600 点环形缓冲、统计值 UI 和失败隔离；当前机器 312 个采样项连续两轮零失败。
- 2026-08-28：完成最多 6 项选择、5 分钟、15 分钟和 1 小时 Swift Charts 曲线、单位分图、保留极值和间隙的显示降采样，并用 312 个序列加速验证 1 小时容量边界。
- 2026-08-29：完成 Debug App 3605 秒真实运行，并叠加 14 核 CPU 满载 600 秒和 12 GiB 内存压力 300 秒；App 未提前退出、无错误日志、无 swap，两个风扇在苹果自动控制下从 0 RPM 升速并在冷却后回到 0 RPM。
- 2026-08-29：完成 Release 性能剖析，消除环形缓冲数组写时复制、逐行重复目录筛选和默认展开 312 行原始候选的渲染开销。同口径 `top` 区间 CPU 均值从约 20.2% 降至约 4.0%，物理内存从约 246 MiB 降至约 195 MiB；312 序列容量测试从 5.520 秒降至 1.689 秒。
- 2026-08-29：补齐菜单栏重开监控窗口入口和一条默认原始温度候选曲线，并完成最终 Release App 3605 秒持续运行。60 个监督样本间隔无缺口、日志为空，长测后普通测试和显式只读硬件探针均通过。
- 2026-08-29：完成低频本地性能标记，覆盖窗口、原始候选展开、曲线选择、时间范围、重新枚举和每分钟状态快照。Debug 实际日志读回与普通测试通过。
- 2026-08-29：完成只读监控产品界面重构，新增概览与传感器侧栏、P 核温度族摘要、菜单栏紧凑温度与双风扇 RPM、逐风扇弹出面板和三列核心指标。当前 Mac16,7 的 `Tp` 族平均温度在短时 CPU 负载中从 33.8 °C 升至 75.3 °C，验证其响应性但不提供逐 key 语义保证。
- 2026-08-29：根据用户最终选择移除主窗口侧栏，改为单页摘要、趋势和可展开传感器区；菜单栏弹窗改为蓝色温度主状态、紧凑分组信息行和底部主按钮。该版 Release 构建通过，主窗口完成本机截图检查，弹窗仍保留人工验收门禁。
- 2026-08-29：完成 App 侧 Turbo 协调器与主窗口、菜单栏两处控件，加入固定 600 秒契约校验、重复启动拒绝、停止确认和失败不误报测试。当前使用明确禁用的 unavailable client，不包含 helper、SMC 写入或管理员授权。
- 2026-08-29：冻结 App、helper 与 Mach service 身份，完成协议 v1 安全编码 payload、双向签名 requirement、实际 App XPC client、只返回写入链路未启用的 helper executable 和现代 LaunchDaemon bundle 布局。未签名 Debug 构建及 39 项普通测试通过；未注册 helper、未请求管理员权限、未写 SMC。
- 2026-08-29：使用当前开发机的免费 Personal Team 完成 App 与 helper 同 Team 签名。双方实际 identifier、相同非空 Team ID 和完整 Apple Development 证书链均已读回，App bundle 通过 deep strict 验证；39 项普通测试为 38 项通过、1 项硬件探针按设计跳过、0 项失败。未注册或启动 helper，未请求管理员权限，未写 SMC。
- 2026-08-29：完成 `SMAppService` 注册状态模型、显式注册确认、待批准系统设置引导和 XPC 查询门禁。签名 Debug App 的实际界面读回首次注册按钮；43 项普通测试为 42 项通过、1 项硬件探针按设计跳过、0 项失败。本轮未执行注册、批准、helper 启动或 SMC 写入。
- 2026-08-29：用户显式完成 LaunchDaemon 注册与系统允许，系统读回安全 stub 由 ServiceManagement 管理、root 运行且 XPC 活跃；App 收到 stub 的 write-path-unavailable，证明注册和双向签名 XPC 通路可达。没有写 SMC。
- 2026-08-29：完成 helper 侧固定 600 秒租约、逐风扇动态模式和最大目标适配器、root-owned 持久租约、每连接所有权、1 秒看门狗、唤醒和重启恢复，以及自动化故障模型。只读探针确认当前 Mac16,7 使用 `F0Md`、`F1Md`、`F0Tg`、`F1Tg` 与 `Ftst`，旧式 `FS! ` 不存在；55 项普通测试为 54 项通过、1 项硬件探针按设计跳过、0 项失败。新 helper 未升级且没有真实 SMC 写入。
- 2026-08-29：在用户明确授权升级但不启动 Turbo 后，完成最终 Personal Team Debug helper 的异步注销后重注册。系统读回 root helper 由 ServiceManagement 管理且 Mach endpoint 活跃，App 经双向签名 XPC 取得无错误 inactive 状态；租约文件不存在，升级后 57 项只读硬件测试全部通过，风扇模式与 `Ftst` 保持 0。没有调用 Turbo 或写 SMC。
- 2026-08-30：在用户明确授权下执行首次短时真实 Turbo 测试。启动前只读门禁通过，激活阶段监控到最高约 2234 RPM 的短时上升，但 helper 因 readback mismatch 进入 failed-safe-auto，没有返回 active。失败后租约已删除，独立复验确认两台风扇模式与 `Ftst` 为 0、实际 RPM 有效、目标已回到苹果自动值；未重试启动。
- 2026-08-30：用户显式升级协议 v6 helper 并完成快速接管短测。`Ftst` 约 0.98 秒后稳定接管，两台风扇约 8.78 秒后取得实际 RPM 上升证据；用户主动停止后恢复日志无错误、租约删除，事后只读门禁确认 Apple 管理模式与 `Ftst=0`。
- 2026-08-30：完成协议 v7 安全审查，修复恢复失败延迟重试、未知 `Ftst` 写入、并发 XPC 请求争用与多风扇状态项扩张。提交 `934721e` 的完整普通测试 74 项通过、4 项按设计跳过，独立只读恢复门禁 1 项通过，v7 Personal Team App 与内嵌 helper 签名构建通过；未升级系统 helper 或执行新的 Turbo。
- 2026-08-30：在第二台 Mac17,2、Apple M5 MacBook Pro 完成单风扇只读适配。探针读回 1 台风扇、小写 `F0md`、动态最大值、P/E 温度族和两枚电池温度；菜单栏右列改为单风扇垂直居中、双风扇上下排列。M4 Pro 与 M5 的完整普通测试均为 76 项通过、4 项按设计跳过；M5 临时无签名 App 已创建菜单栏状态项并完成首次扫描，没有安装 helper 或尝试 Turbo。
